# EC2 / Production: all secrets are fetched from AWS SSM Parameter Store by default.
# No .env file is needed on EC2 — just ensure the instance has IAM permissions to read SSM.
#
# SSM parameters used:
#   /fitted/openrouter-api-key
#   /fitted/weather-api-key
#   /fitted/database-url
#   /fitted/jwt-secret-key
#   /fitted/jwt-algorithm         (default: HS256)
#   /fitted/access-token-expire-minutes (default: 1440)
#   /fitted/ses-sender-email           (dev default: noreply@example.com)
#   /fitted/frontend-url               (dev default: http://localhost:5001)
#
# Environment variables (injected by SAM, not SSM):
#   WEATHER_BUCKET_NAME           (set by !Ref WeatherDataBucket in template.yaml)
#
# Local development — create a .env file with these vars.
#
# Recommended: USE_SSM=true (requires AWS credentials with SSM read access).
# SSM is the single source of truth — no need to mirror secrets here.
# Only set USE_SSM=false if you don't have AWS credentials locally.

USE_SSM=true
WEATHER_BUCKET_NAME=<your-bucket>
# Password reset (dev only — set DISABLE_EMAIL=true to skip SES entirely)
DISABLE_EMAIL=true
SES_SENDER_EMAIL=testertestertestingtested@gmail.com

# Only needed when USE_SSM=false:
# DATABASE_URL=postgresql://fitted_admin:<password>@<rds-endpoint>:5432/fitted
# OPENROUTER_API_KEY=<your-key>
# WEATHER_API_KEY=<your-key>
# JWT_SECRET_KEY=dev-secret-key-change-me-in-prod
# SERPER_API_KEY=<your-key>

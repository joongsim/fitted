# Copy this file to .env on EC2 and fill in DATABASE_URL.
# All other secrets (API keys, JWT) are pulled from SSM Parameter Store automatically.

USE_SSM=true
DATABASE_URL=postgresql://fitted_admin:<password>@<rds-endpoint>:5432/fitted

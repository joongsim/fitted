# Core API dependencies
fastapi
mangum  # AWS Lambda adapter for FastAPI
httpx
python-dotenv
openai

# Development dependencies (not needed in Lambda)
uvicorn[standard]

# Data pipeline dependencies (for Airflow/dbt, not Lambda)
databricks-cli
dbt-databricks